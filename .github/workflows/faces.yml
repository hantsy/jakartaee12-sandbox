name: Build faces

on:
  push:
    paths:
      - "faces/**"
      - "pom.xml"
    branches:
      - master
  pull_request:
    types:
      - opened
      - synchronize
      - reopened

jobs:
  faces:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - name: Set up JDK
      uses: actions/setup-java@v5
      with:
        java-version: '25'
        distribution: 'zulu'
        cache: maven
    - name: Build with Maven
      run: |
        cd faces
        mvn -B -q clean verify -Parq-glassfish-managed
    - uses: actions/upload-artifact@v6
      if: failure()
      with:
        name: server-log
        path: faces/target/glassfish*/glassfish/domains/domain1/logs/server.log
